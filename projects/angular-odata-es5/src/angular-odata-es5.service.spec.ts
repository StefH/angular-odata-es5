/*
import { TestBed } from '@angular/core/testing';

import { AngularOdataEs5Service } from './angular-odata-es5.service';

describe('AngularOdataEs5Service', () => {
  let service: AngularOdataEs5Service;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(AngularOdataEs5Service);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
*/