/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AngularOdataEs5Component } from './angular-odata-es5.component';

describe('AngularOdataEs5Component', () => {
  let component: AngularOdataEs5Component;
  let fixture: ComponentFixture<AngularOdataEs5Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AngularOdataEs5Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(AngularOdataEs5Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/